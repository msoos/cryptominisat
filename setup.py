# This file is intentionally empty.
# pycryptosat is now built with scikit-build-core (see pyproject.toml).
# Install with:  pip install .
raise SystemExit(
    "Direct invocation of setup.py is no longer supported.\n"
    "Use:  pip install ."
)
